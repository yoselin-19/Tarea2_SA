<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous")
        title Pedido de comida
    body
        h1(style="text-align: center;") Bienvenido a su aplicacion para hacer pedidos
        div(style="background-color: lightblue;")
            h1(style="text-align: center;") ---

        div(style="text-align: center;")
            h1 Lista de Platillos
            h3 Seleccione alguno de los platillos

            div(style="text-align: center;")
                form(action="/make_pedido", method="POST")
                    div(style="text-align: center;")
                        select(name="platillos" id="plat")
                            if platillos
                                for platillo in platillos
                                    option(value=platillo.name) #{platillo.name} - #{platillo.precio}
                            else
                                option(value="default") No disponible- 00.00
                    br
                    br
                    div(style="text-align: center;")
                        input(type="submit", value="Hacer pedido" class="btn btn-success")